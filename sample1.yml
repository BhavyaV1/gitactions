name: CI/CD pipeline
on: push
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Build
      run: |
        echo "Building the code..."
    - name: Test
      run: |
        echo "Running tests..."
